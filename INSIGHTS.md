## 2026-08-25 · gotcha
**Phase 6.2's install rehearsal in `plugin-install-target` produced a `workflow-retro` action item claiming the installed `sdd-engineering:implementer` had no `Bash` tool at runtime — but its frontmatter (`plugins/sdd-engineering/agents/implementer.md:12`) declares `tools: Read, Grep, Glob, Edit, Write, Bash, Skill`, Bash included. Not resolved; flagged for a future session, not re-investigated here to avoid burning more subscription budget on a non-blocking discrepancy.**
The rehearsal itself succeeded (33/33 tests, clean plan-verifier/architecture-reviewer review gate, real namespaced dispatch of all five `sdd-engineering`/`architecture-review` agents) — the orchestrating session just had to run `npm test` manually instead of `implementer` doing it inside the automated loop. Two explanations are equally plausible without further digging: (a) the retro's own LLM-generated summary misread the transcript (a real risk — `workflow-retro` synthesizes a narrative from the trace, it doesn't literally diff `allowedTools`), or (b) something about installed-via-marketplace dispatch (vs. this repo's own subagent dispatch, or vs. running from a checked-out `plugins/` directory) actually stripped `Bash` at runtime despite the frontmatter granting it — which, if true, would echo Phase 4's own finding that `Options.allowedTools` alone doesn't restrict/grant tools the way it looks like it should (`evals/src/runtime/run-claude.ts`'s `Options.tools` fix). Worth a targeted follow-up rehearsal if this recurs.
Evidence: plugin-install-target/docs/retros/sessions/496575b8.md:6 (retro's claim); plugins/sdd-engineering/agents/implementer.md:12 (frontmatter, Bash present)

## 2026-08-25 · gotcha
**A sub-plan's "light pass, no rewrite expected" research note for a source file can still miss a real defect — verify on the actual copied file, not the plan's prior full-read finding.**
`marketplace-extraction-phase2-4-sdd-engineering.md` Step 2 claimed `plan-verifier.md` had "zero DevDigest references, no hidden language mandate" and needed no rewrite. A fresh full read of the copied file during Step 4 found it still bare-named `architecture-reviewer` (needed the `architecture-review:` namespace, per the cross-plugin-reference invariant) and cited `pr-self-review` — a skill this marketplace's architecture spec deliberately does not extract — in its "Hard boundary" section. Both were fixed in the copy. The lesson: even a file a plan calls "confirm only" still needs the same full read the plan applied to the heavy-rewrite files, because "no DevDigest references" and "no cross-plugin/unextracted-skill reference to fix" are different checks.
Evidence: plugins/sdd-engineering/agents/plan-verifier.md:63 (post-fix); source dev-digest/.claude/agents/plan-verifier.md:63 (pre-fix, still bare `architecture-reviewer`/`pr-self-review`)

## 2026-08-25 · decision
**Same-plugin agent/skill cross-references (`plan-verifier`, `implementer`, `engineering-insights` all inside `sdd-engineering`) were left as bare names, not `sdd-engineering:`-prefixed — confirmed correct by `claude plugin validate`.**
The source plan explicitly deferred this convention question to an empirical validation run. Phase 5's Step 4 (`claude plugin validate ./plugins/sdd-engineering` and `./plugins/architecture-review`, run against the diff-artifact-reuse trim, which itself added a new bare same-plugin pointer — `plan-verifier.md`'s trimmed text now says "the diff-artifact-reuse convention `run-plan` documents in full", bare, same-plugin) settled it: both plugins validate clean with bare same-plugin references left as-is. Cross-plugin references (`engineering-paved-path:onion-architecture`, `research-tools:researcher`, `architecture-review:architecture-reviewer`, and the trim's own new `sdd-engineering:run-plan` pointer from `architecture-reviewer.md`) stay namespaced; same-plugin references stay bare. This is now confirmed, not provisional.
Evidence: plugins/sdd-engineering/agents/plan-verifier.md:30 (bare `run-plan`, post-Phase-5-trim); plugins/architecture-review/agents/architecture-reviewer.md:31 (namespaced `sdd-engineering:run-plan`, cross-plugin); both `claude plugin validate` runs clean, 2026-08-25.

## 2026-08-25 · gotcha
**3.2's `useT` stub took `t(key, fallback)`, but architecture.md:344 specifies a single-arg `useT('key')` hook — every 3.2 call site had to be edited in 3.3, not just the hook internals.**
The 3.2 stub (`site/src/i18n/useT.ts`, pre-3.3) returned the caller-supplied `fallback` string directly so components "already route strings through `t`" — but its two-arg shape doesn't match the spec's `useT('key')` single-arg contract, and TypeScript won't silently accept a stale two-arg call once the real hook's signature drops the second parameter (calling a function with an extra argument is a compile error). Swapping the hook alone would have left every 3.2 page failing `tsc`. Confirm the *target* hook signature against the spec before assuming a "no call-site changes required" comment in a placeholder still holds once the real implementation lands.
Evidence: site/src/i18n/useT.ts:16 (real hook, single-arg); site/src/pages/HomePage.tsx:15 (call site rewritten from `t('home.loading', 'Loading catalog…')` to `t('home.loading')`)

## 2026-08-25 · decision
**`index.json`'s `PluginEntry` had no structured `COMPATIBILITY.md` floor field before Phase 3.3 — added `compatibilityFloor` to `build-index.mjs` rather than parsing the raw compatibility body client-side.**
3.1's `PluginPage.tsx` comment explicitly said "COMPATIBILITY.md is not surfaced as structured data in index.json... rendered here as its own sanitized markdown section rather than parsed into a floor value that doesn't exist." 3.3's `CompatibilityBadge` needs that floor as data (real-data-only constraint forbids hardcoding `>=2.1.110`), so `parseCompatibilityFloor()` was added to `build-index.mjs` (regex on the `` Requires Claude Code `>=X.Y.Z`. `` sentence) and the parsed value flows through `index.json` → `PluginEntry.compatibilityFloor`. The raw markdown body is still fetched and rendered separately for prose — the two paths (structured floor vs. prose body) now both read the same `COMPATIBILITY.md` fetch in `buildCatalog()`.
Evidence: scripts/build-index.mjs:263 (parseCompatibilityFloor), scripts/build-index.mjs:317 (wired into the PluginEntry.dependencies output)

## 2026-08-25 · gotcha
**`scripts/build-index.mjs`'s `parseChangelog()` silently drops any heading that isn't exactly `## <version> — <title>` (em dash, U+2014) — a leftover `## Unreleased` heading from a cost-optimization pass is not cosmetic, it's a catalog regression.**
Phase 5 left `## Unreleased` sections in `plugins/architecture-review/CHANGELOG.md` and `plugins/sdd-engineering/CHANGELOG.md` (the diff-artifact-reuse trim notes). `parseChangelog`'s regex (`^##\s+(\S+)\s+—\s+(.+)$`) only matches the versioned-heading shape, so an `## Unreleased` section is silently excluded from `releases.json`'s `#/whats-new` feed rather than erroring — the plugin's entire notes for that pass would vanish from the published catalog with no build failure to catch it. Fixed by folding each `Unreleased` entry's content into the plugin's existing `## 1.0.0 — Initial extraction` section as an additional paragraph, before any release tag was created (Phase 6.1, Step 1).
Evidence: scripts/build-index.mjs:172-173 (regex), scripts/build-index.mjs:344-349 (`releases[pluginName] = changelogEntries.map(...)`); plugins/architecture-review/CHANGELOG.md, plugins/sdd-engineering/CHANGELOG.md (post-fold-in, single `## 1.0.0 —` heading each)
